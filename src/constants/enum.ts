export enum Mode {
  BEARER = "BEARER",
  REFRESH = "REFRESH",
}

export enum Role {
  SUPER_ADMIN = "SUPER_ADMIN",
  ADMIN = "ADMIN",
  USER = "USER",
}
export enum RoleLevel {
  ZERO = 0,
  ONE = 1,
  TWO = 2,
  THREE = 3,
}

export enum TokenStatus {
  ACTIVE = "ACTIVE",
  INACTIVE = "INACTIVE",
}
export enum MailTemplate {
  OTP = "otp",
}
